## Packages
recharts | Dashboard analytics charts
framer-motion | Smooth transitions and animations for ticket cards
date-fns | Date formatting for SLA and timestamps
lucide-react | Icons (already in stack but confirming usage)
clsx | Class name utility (already in stack)
tailwind-merge | Class merging (already in stack)

## Notes
Technician view is mobile-first
Admin dashboard uses charts
Auth state is handled via query cache
