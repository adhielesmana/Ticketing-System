#====================================================================
# NetGuard ISP - Deployment Configuration
# Copy to .env and edit before running deploy.sh
# Or pass as environment variables: DOMAIN=mysite.com ./deploy.sh
#====================================================================

# Your domain name (required - point DNS to your server first)
DOMAIN=tickets.yourdomain.com

# Email for SSL certificate notifications
SSL_EMAIL=admin@yourdomain.com

# Application name (used for service name, nginx config, Docker names)
APP_NAME=netguard

# Ports - change if these conflict with existing services
APP_PORT=3100
DB_PORT=5433

# Database credentials (auto-generated if not set)
DB_NAME=netguard_db
DB_USER=netguard
DB_PASS=

# Session secret (auto-generated if not set)
SESSION_SECRET=

# Installation directory
INSTALL_DIR=/opt/netguard
